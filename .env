# Site URL used for canonical links, Open Graph tags, JSON-LD and sitemap.xml.
# The GitHub Pages workflow sets it; locally it defaults to the `vite preview` server.
# Uncomment to override, e.g. for a custom domain:
# VITE_SITE_URL=https://www.example.com
