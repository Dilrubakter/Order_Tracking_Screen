# Public origin used for canonical URLs, Open Graph tags, JSON-LD and sitemap.xml.
# Leave unset: on Vercel it defaults to the project's production URL, locally to
# the `vite preview` server. Set it only for a custom domain, e.g.
# VITE_SITE_URL=https://www.example.com
