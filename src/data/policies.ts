import type { Money } from '@/types/order';

/** Goodwill credit offered when a delayed customer chooses to keep waiting. */
export const DELAY_CREDIT: Money = { amount: 500, currency: 'BDT' };

export const SUPPORT_PHONE_DISPLAY = '+880 9611-555019';
export const SUPPORT_PHONE_TEL = '+8809611555019';
